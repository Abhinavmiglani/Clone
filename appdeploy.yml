apiVersion: v1
kind: Pod
metadata:
   name: backend-pod
spec:
   containers:
   - name: app-container01
     image: https://hub.docker.com/repository/docker/sameermosesmurala/backendapplication
     ports:
     - containerPort: 8082
